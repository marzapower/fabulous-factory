#!/usr/bin/env tsx
/**
 * `pnpm db:seed` — M2 stub. Demo seed data ships with the demo product itself, which
 * lands in M6 (the golden-path mini page monitor). Nothing to seed yet; this exists so
 * the command is discoverable ahead of time and always succeeds.
 */
console.log("db:seed: demo seed data ships with the demo product (M6). Nothing to seed yet.");
process.exitCode = 0;
