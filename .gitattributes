# Force LF line endings everywhere and let git auto-detect text vs. binary — ordinary
# cross-platform diff hygiene, so a CRLF checkout on Windows doesn't turn every text file
# it touches into a noisy whole-file diff (M9, opt-14).
* text=auto eol=lf
